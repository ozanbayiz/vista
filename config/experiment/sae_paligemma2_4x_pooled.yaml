# @package _global_

# Experiment: Train 4x BatchTopK SAE on PaliGemma2 pooled FairFace latents
# Usage: python -m src.main +experiment=sae_paligemma2_4x_pooled

defaults:
  - override /model: sae_paligemma2
  - override /data: fairface_paligemma2_pooled_no_labels
  - override /module: sae

trainer:
  max_epochs: 50
  precision: 16-mixed

wandb:
  project: idarve
  enabled: true
